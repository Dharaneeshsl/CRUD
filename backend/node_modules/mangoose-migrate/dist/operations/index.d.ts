import { Connection } from 'mongoose';
import { M as MigrationOperation } from '../types-CbeFXqzM.js';

declare class CreateModel implements MigrationOperation {
    private readonly modelName;
    private readonly schemaDefinition;
    constructor(modelName: string, schemaDefinition: Record<string, any>);
    applyUp(connection: Connection): Promise<void>;
    applyDown(connection: Connection): Promise<void>;
}

declare class AddField implements MigrationOperation {
    private readonly modelName;
    private readonly fieldName;
    private readonly fieldDefinition;
    constructor(modelName: string, fieldName: string, fieldDefinition: any);
    applyUp(connection: Connection): Promise<void>;
    applyDown(connection: Connection): Promise<void>;
}

export { AddField, CreateModel };
